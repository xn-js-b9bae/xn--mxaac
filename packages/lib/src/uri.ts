export const encodeTestPath = (path: string): string =>
	encodeURIComponent(path);
export const decodeTestPath = (uriComponent: string): string =>
	decodeURIComponent(uriComponent);

export const encodeTestTitle = (title: string): string =>
	encodeURIComponent(title);
export const decodeTestTitle = (uriComponent: string): string =>
	decodeURIComponent(uriComponent);
